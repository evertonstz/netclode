# K3s containerd configuration with NVIDIA runtime support
# This file extends K3s default containerd config
#
# K3s uses config.toml.tmpl as a template that gets merged with defaults.

[plugins."io.containerd.grpc.v1.cri".containerd]
  default_runtime_name = "kata-clh"

# Kata Cloud Hypervisor runtime (for sandboxed agents)
[plugins."io.containerd.grpc.v1.cri".containerd.runtimes.kata-clh]
  runtime_type = "io.containerd.kata-clh.v2"
  privileged_without_host_devices = true

# Kata runtime alias for backwards compatibility
[plugins."io.containerd.grpc.v1.cri".containerd.runtimes.kata]
  runtime_type = "io.containerd.kata-clh.v2"
  privileged_without_host_devices = true

# NVIDIA runtime configuration (for Ollama GPU workloads)
[plugins."io.containerd.grpc.v1.cri".containerd.runtimes.nvidia]
  runtime_type = "io.containerd.runc.v2"

[plugins."io.containerd.grpc.v1.cri".containerd.runtimes.nvidia.options]
  BinaryName = "/usr/bin/nvidia-container-runtime"

# Regular containerd runtime (non-isolated)
[plugins."io.containerd.grpc.v1.cri".containerd.runtimes.runc]
  runtime_type = "io.containerd.runc.v2"
