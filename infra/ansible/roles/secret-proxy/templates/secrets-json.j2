{
{% if anthropic_api_key | length > 0 %}
  "NETCLODE_PLACEHOLDER_anthropic": {
    "secret": "{{ anthropic_api_key }}",
    "hosts": ["api.anthropic.com"]
  }{% if openai_api_key | length > 0 or mistral_api_key | length > 0 %},{% endif %}

{% endif %}
{% if openai_api_key | length > 0 %}
  "NETCLODE_PLACEHOLDER_openai": {
    "secret": "{{ openai_api_key }}",
    "hosts": ["api.openai.com"]
  }{% if mistral_api_key | length > 0 %},{% endif %}

{% endif %}
{% if mistral_api_key | length > 0 %}
  "NETCLODE_PLACEHOLDER_mistral": {
    "secret": "{{ mistral_api_key }}",
    "hosts": ["api.mistral.ai"]
  }
{% endif %}
}
