{
  "name": "netclode",
  "private": true,
  "workspaces": [
    "services/*"
  ],
  "scripts": {
    "dev:agent": "npm run dev --workspace=@netclode/agent",
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
